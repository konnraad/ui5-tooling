import{_ as e,c as i,o as a,a4 as t}from"./chunks/framework.B94ikmwp.js";const f=JSON.parse('{"title":"Code Analysis","description":"","frontmatter":{},"headers":[],"relativePath":"CodeAnalysis.md","filePath":"CodeAnalysis.md"}'),o={name:"CodeAnalysis.md"},n=t(`<h1 id="code-analysis" tabindex="-1">Code Analysis <a class="header-anchor" href="#code-analysis" aria-label="Permalink to &quot;Code Analysis&quot;">​</a></h1><p>During the build process, UI5 Tooling executes a static code analysis of your project. In the following sections the term &quot;simple literal&quot; is used for the following language features:</p><ul><li>string literals</li><li>numeric literals</li><li>Boolean literals</li><li><code>null</code> / <code>undefined</code></li><li>template literals without any expressions</li><li>arrays of the previous (spread operators are ignored)</li><li>object literals with &quot;simple literals&quot; used as keys and values (again, spread operators are ignored)</li></ul><h2 id="dependency-analysis" tabindex="-1">Dependency Analysis <a class="header-anchor" href="#dependency-analysis" aria-label="Permalink to &quot;Dependency Analysis&quot;">​</a></h2><p>UI5 Tooling extracts dependency information from a project&#39;s code as outlined in the following sections. The APIs described there require the usage of &quot;simple literals&quot; when declaring dependencies.</p><h3 id="jsmodule-analyzer" tabindex="-1">JSModule Analyzer <a class="header-anchor" href="#jsmodule-analyzer" aria-label="Permalink to &quot;JSModule Analyzer&quot;">​</a></h3><p>The following APIs are analyzed by the JSModule Analyzer:</p><ul><li>sap.ui.define</li><li>sap.ui.require</li><li>jQuery.sap.declare (deprecated)</li><li>jQuery.sap.require (deprecated)</li><li>sap.ui.requireSync (deprecated)</li><li>sap.ui.preload (restricted)</li><li>sap.ui.require.preload (restricted)</li></ul><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/JSModuleAnalyzer.js" target="_blank" rel="noreferrer">JSModule Analyzer</a> uses the <a href="https://en.wikipedia.org/wiki/Abstract_syntax_tree" target="_blank" rel="noreferrer">&quot;Abstract Syntax Tree&quot;</a> (AST) of a JavaScript file to decide whether a code block is executed <em>conditionally</em> or <em>unconditionally</em>.</p><p>The analyzer uses a set of rules to decide whether one of the above APIs is called whenever the module is executed or whether the API is only called under certain conditions.</p><p>For example, top-level code is always executed. Flow-control statements in JavaScript imply that certain blocks of code are only executed under certain conditions (for example, <code>if</code> blocks, <code>else</code> blocks, ...). Besides these inherent JavaScript rules, further common patterns are known to the analyzer, e.g. immediately invoked function expressions or the factory function of AMD modules.</p><p>Any dependencies found that - according to these rules - are always executed, are collected as eager (or standard) dependencies. Dependencies that are found on a code path that depends on certain conditions are collected as conditional dependencies.</p><p>The bundling implemented by UI5 Tooling can either follow only eager dependencies (<code>resolve</code>: <code>true</code>) or additionally conditional dependencies (<code>resolveConditional</code>). For more information, see <a href="https://sap.github.io/ui5-tooling/v4/pages/Configuration/#custom-bundling" target="_blank" rel="noreferrer">Custom Bundling</a>.</p><p>When a dependency in one of the mentioned APIs is not a &quot;simple literal&quot; but an expression, the corresponding module is marked as &quot;having dynamic dependencies&quot;. This marker is currently not further evaluated by the tooling.</p><h3 id="component-analyzer" tabindex="-1">Component Analyzer <a class="header-anchor" href="#component-analyzer" aria-label="Permalink to &quot;Component Analyzer&quot;">​</a></h3><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/ComponentAnalyzer.js" target="_blank" rel="noreferrer">Component Analyzer</a> analyzes JavaScript files named <code>Component.js</code> to collect dependency information by searching for a <code>manifest.json</code> located in the same folder as the <code>Component.js</code>. If one is found, the <code>sap.ui5</code> section is evaluated in the following way:</p><ul><li>Any library dependency is added as a dependency to the <code>library.js</code> module of that library. If the library dependency is modeled as &#39;lazy&#39;, the module dependency will be added as &#39;conditional&#39;.</li><li>Any component dependency is added as a dependency to a file named <code>Component.js</code>. If the dependency is modeled as &#39;lazy&#39;, the module dependency will be added as &#39;conditional&#39;.</li><li>For each UI5 model for which a type is configured, a module dependency to that type is added.</li><li>For each route that contains a view name, a module dependency to that view is added.</li></ul><h3 id="smart-template-analyzer" tabindex="-1">Smart Template Analyzer <a class="header-anchor" href="#smart-template-analyzer" aria-label="Permalink to &quot;Smart Template Analyzer&quot;">​</a></h3><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/SmartTemplateAnalyzer.js" target="_blank" rel="noreferrer">Smart Template Analyzer</a> analyzes a Smart Template app and its underlying template components to collect dependency information. It searches for a <code>manifest.json</code> located in the same folder as the <code>Component.js</code>. If it is found and if it is valid JSON, an <code>sap.ui.generic.app</code> section is searched and evaluated in the following way:</p><ul><li>For each page configuration, the configured component is added as a dependency to the template app module.</li><li>If the page configuration contains a <code>templateName</code>, a dependency to that template view is added to the app.</li><li>Otherwise, the class definition of the component is analyzed to find a default template view name. If found, a dependency to that view is added to the app module.</li></ul><p>The template component is analyzed in the following way:</p><ul><li>precondition: the template component class is defined in an AMD-style module, using define or sap.ui.define</li><li>precondition: the module <code>sap/suite/ui/generic/template/lib/TemplateAssembler</code> is imported</li><li>precondition: a call to <code>TemplateAssembler.getTemplateComponent</code> is used to define the component class</li><li>precondition: that call is used in a top-level return statement of the factory function</li><li>precondition: necessary parameters to that call are given as an object literal (no further coding)</li><li>precondition: the settings define a managed property <code>metadata.properties.templateName</code> with a defaultValue of type string The default value of the property represents the template view of the template component. The manifest of the template app could in theory specify an alternative template in <code>component.settings.templateName</code>.</li></ul><h3 id="xml-template-analyzer" tabindex="-1">XML Template Analyzer <a class="header-anchor" href="#xml-template-analyzer" aria-label="Permalink to &quot;XML Template Analyzer&quot;">​</a></h3><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/XMLTemplateAnalyzer.js" target="_blank" rel="noreferrer">XML Template Analyzer</a> tackles <code>XMLView</code> and <code>XMLFragment</code>. It parses the XML, collects controls, and adds them as dependency to the ModuleInfo object. Additionally, some special dependencies are handled:</p><ul><li>controller of the view</li><li>resource bundle (note: as locale-dependent dependencies can&#39;t be modelled yet in the ModuleInfo, only a dependency to the development version (aka raw language) of the bundle will be added)</li><li>component referenced via the ComponentContainer control</li><li>embedded fragments or views</li></ul><p>In an XMLView/XMLFragment, usually three categories of element nodes exist: Controls, aggregations, and non-UI5 nodes (e.g. XHTML or SVG). The third category usually can be identified by its namespace. To distinguish between the first two categories, the analyzer checks whether a resource exists in the project or its dependencies whose name matches the qualified node name. For example, if the qualified node name is <code>sap.m.Text</code>, the analyzer checks whether <code>sap/m/Text.js</code> exists. If so, UI5 Tooling detects this node as a control, otherwise as an aggregation.</p><h3 id="xml-composite-analyzer" tabindex="-1">XML Composite Analyzer <a class="header-anchor" href="#xml-composite-analyzer" aria-label="Permalink to &quot;XML Composite Analyzer&quot;">​</a></h3><p>The <strong>XMLComposite</strong> control is deprecated since version UI5 1.88. Nevertheless, UI5 Tooling will attempt to analyze the declaration of any such controls in a project.</p><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/XMLCompositeAnalyzer.js" target="_blank" rel="noreferrer">XML Composite Analyzer</a> searches for the name of the configured fragment containing the <strong>XMLComposite</strong> control.</p><div class="vp-code-group vp-adaptive-theme"><div class="tabs"><input type="radio" name="group-f48zH" id="tab-f3lKB__" checked><label for="tab-f3lKB__">Name of the XMLComposite is equal to fragment name</label><input type="radio" name="group-f48zH" id="tab-NJynmvB"><label for="tab-NJynmvB">Dedicated fragment name</label></div><div class="blocks"><div class="language-javascript vp-adaptive-theme active"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sap.ui.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">define</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sap/ui/core/XMLComposite&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">XMLComposite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line highlighted"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> XMLComposite.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">extend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;composites.MyComposite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {} </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sap.ui.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">define</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;sap/ui/core/XMLComposite&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">XMLComposite</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> XMLComposite.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">extend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;composites.MyComposite&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line highlighted"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        fragment: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;composites.custom.MyComposite&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div></div></div><h2 id="library-initialization" tabindex="-1">Library Initialization <a class="header-anchor" href="#library-initialization" aria-label="Permalink to &quot;Library Initialization&quot;">​</a></h2><p>The <a href="https://github.com/SAP/ui5-builder/blob/main/lib/lbt/analyzer/analyzeLibraryJS.js" target="_blank" rel="noreferrer">library.js Analyzer</a> checks every <code>library.js</code> file in the namespace of a library for occurences of a <code>sap/ui/core/Core#initLibrary</code> call. If found, the following information will be placed in the generated manifest.json:</p><ul><li>noLibraryCSS: false when the noLibraryCSS property had been set in the initLibrary info object</li><li>types: string array with the names of the types contained in the library</li><li>controls: string array with the names of the controls defined in the library</li><li>elements: string array with the names of the elements defined in the library</li><li>interfaces: string array with the names of the interfaces defined in the library</li></ul><p><code>sap/ui/core/Core#initLibrary</code> requires the usage of &quot;simple literals&quot; for the parameters passed to this function call.</p><p>Note: Currently only the usage via the global <code>sap.ui.getCore().initLibrary</code> is supported by this analyzer. Requiring <code>sap/ui/core/Core</code> and then calling <code>Core.initLibrary</code> is not recognized by this analyzer.</p><h2 id="jsdoc" tabindex="-1">JSDoc <a class="header-anchor" href="#jsdoc" aria-label="Permalink to &quot;JSDoc&quot;">​</a></h2><p>The UI5 Tooling offers a JSDoc build, which is enhanced by UI5-specific JSDoc features.</p><p>An UI5 SDK can be built locally. To get more insight into the local UI5 SDK build setup, have a look at our <a href="https://github.com/SAP/openui5/blob/-/docs/developing.md#building-the-openui5-sdk-demo-kit" target="_blank" rel="noreferrer">Developer&#39;s Guide</a>.</p><p>Currently, the resources needed for a UI5 SDK build are stored in <a href="https://github.com/SAP/openui5/tree/-/lib/jsdoc" target="_blank" rel="noreferrer">openui5</a> and in <a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc" target="_blank" rel="noreferrer">ui5-builder</a>. This double maintenance is needed because these files are not part of the <code>sap.ui.core</code> library artefact, so building JSDoc for any library has no access to the needed resources. It&#39;s therefore necessary to have these resources also available in the <em>ui5-builder</em>. This might change in the future.</p><p>The following artefacts contribute to the JSDoc build:</p><ul><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/jsdocGenerator.js" target="_blank" rel="noreferrer">jsdocGenerator.js</a>: Executes the actual JSDoc build. UI5 Tooling wrapper for <code>plugin.cjs</code>.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/sdkTransformer.js" target="_blank" rel="noreferrer">sdkTransformer.js</a>: UI5 Tooling wrapper for <code>transformApiJson.cjs</code>.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/apiIndexGenerator.js" target="_blank" rel="noreferrer">apiIndexGenerator.js</a>: UI5 Tooling wrapper for <code>createIndexFiles.cjs</code>.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/lib/createIndexFiles.cjs" target="_blank" rel="noreferrer">createIndexFiles.cjs</a>: Creates API index resources from all <code>api.json</code> resources and by interpreting the <code>sap-ui-version.json</code> available in the given test resources directory. The resulting index resources (e.g. <code>api-index.json</code>, <code>api-index-deprecated.json</code>, <code>api-index-experimental.json</code> and <code>api-index-since.json</code>) are only to be used in a UI5 SDK.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/lib/transformApiJson.cjs" target="_blank" rel="noreferrer">transformApiJson.cjs</a>: Preprocesses <code>api.json</code> files for use in UI5 SDKs. Transforms the <code>api.json</code> as created by the JSDoc build into a pre-processed <code>api.json</code> file suitable for the SDK. The pre-processing includes formatting of type references, rewriting of links, and other time-consuming calculations.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/lib/ui5/plugin.cjs" target="_blank" rel="noreferrer">plugin.cjs</a>: UI5 plugin for JSDoc3. The plugin adds the following UI5-specific tag definitions to JSDoc3:</p><ul><li>disclaimer</li><li>experimental</li><li>final</li><li>interface</li><li>implements</li><li>ui5-restricted, and more</li></ul><p>It furthermore listens to the following JSDoc3 events to implement additional functionality:</p><ul><li>parseBegin: to create short names for all files that are to be parsed</li><li>fileBegin: to write to the log (a kind of progress indicator)</li><li>jsdocCommentFound: to pre-process comments, empty lines are used as paragraph markers, a default visibility is added, and legacy tag combinations used in JSDoc2 are converted to JSDoc3 conventions</li><li>newDoclet</li><li>parseComplete: remove undocumented/ignored/private doclets or duplicate doclets</li></ul><p>Last but not least, it implements an astNodeVisitor to detect UI5-specific &quot;extend&quot; calls and to create documentation for the properties, aggregations, etc. that are created with the &quot;extend&quot; call.</p></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/lib/ui5/template/publish.cjs" target="_blank" rel="noreferrer">publish.cjs</a>: JSDoc3 template for UI5 documentation generation.</p><ul><li>adds missing namespaces</li><li>determines the export names of exported APIs</li><li>writes out the <code>api.json</code> from the collected JSDoc information</li><li>calculates the inheritance hierarchy</li><li>checks for cyclic dependencies</li><li>removes unnecessary whitespace from an HTML document</li></ul></li><li><p><a href="https://github.com/SAP/ui5-builder/blob/main/lib/processors/jsdoc/lib/ui5/template/utils/versionUtil.cjs" target="_blank" rel="noreferrer">versionUtil.cjs</a>: Provides helper methods to determine version-related information.</p></li></ul>`,41),s=[n];function l(r,d,c,p,h,u){return a(),i("div",null,s)}const y=e(o,[["render",l]]);export{f as __pageData,y as default};
